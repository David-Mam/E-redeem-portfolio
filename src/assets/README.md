# Bundled Assets Directory

Place assets here if you plan to import them inside your TypeScript/React components (e.g., `import logo from '@/assets/images/logo.svg'`).

For direct URLs without importing (e.g., `<img src="/assets/images/hero.jpg" />`), use the `public/assets/` directory instead.
